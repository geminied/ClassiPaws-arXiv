import Link from "next/link"; import {Bookmark,ExternalLink} from "lucide-react"; import type {Paper} from "@/lib/data";
export function PaperCard({paper}:{paper:Paper}){return <article className="card paper"><div className="paper-head"><div><Link href={`/papers/${paper.id}`}><h3>{paper.title}</h3></Link><small>{paper.authors} · {paper.year}</small></div><span className="badge">{paper.targetClass}</span></div><p>{paper.abstract}</p><div className="actions"><Link href={`/papers/${paper.id}`} className="btn primary small"><ExternalLink size={13}/>View Paper</Link><Link href={`/predict?paper=${paper.id}`} className="btn ghost small">Predict</Link><button className="btn ghost small"><Bookmark size={13}/></button></div></article>}
