"use client"; import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,CartesianGrid,Tooltip} from "recharts"; import type {Model} from "@/lib/data";
export function PerformanceChart({models,metric}:{models:Model[],metric:'macroF1'|'accuracy'|'weightedF1'}){const data=models.map(m=>({name:m.name.length>13?m.name.slice(0,12)+'…':m.name,value:+(m[metric]*100).toFixed(2)}));return <div className="chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={{top:10,right:10,left:-20,bottom:55}}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#eeeaf6"/><XAxis dataKey="name" angle={-25} textAnchor="end" interval={0} tick={{fontSize:9,fill:'#8589a5'}}/><YAxis domain={[0,100]} tick={{fontSize:9,fill:'#8589a5'}}/><Tooltip formatter={(v)=>[`${v}%`,'Score']}/><Bar dataKey="value" fill="#7652ee" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div>}
