import {Brain,Database,Search,Server,Sparkles} from "lucide-react";
export default function About(){return <main className="page"><div className="section-head"><div><span className="pill"><Sparkles size={13}/>About the project</span><h1 style={{marginTop:10}}>Research, classification & model intelligence</h1><p>A creative web interface around the research-paper discipline classification project.</p></div></div><div className="features"><Box icon={<Search/>} title="Discover" text="Search papers and filter by target discipline."/><Box icon={<Brain/>} title="Classify" text="Submit a new title and abstract to the real inference service."/><Box icon={<Server/>} title="Inference" text="FastAPI will host the exported trained models."/></div><section className="card architecture"><Node icon={<Search/>} title="Next.js" text="Interactive frontend"/><b>→</b><Node icon={<Database/>} title="PostgreSQL" text="Papers + metadata"/><b>→</b><Node icon={<Brain/>} title="FastAPI" text="Real ML inference"/></section></main>}
function Box({icon,title,text}:{icon:React.ReactNode,title:string,text:string}){return <article className="card feature"><div className="icon">{icon}</div><h3>{title}</h3><p>{text}</p></article>} function Node({icon,title,text}:{icon:React.ReactNode,title:string,text:string}){return <div className="node"><div className="icon">{icon}</div><b>{title}</b><small>{text}</small></div>}
